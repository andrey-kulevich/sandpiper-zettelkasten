import * as React from 'react';
import * as ReactDOM from 'react-dom';

function render() {
	ReactDOM.render(
		<React.StrictMode>
			<h2>List of notes</h2>
		</React.StrictMode>,
		document.body,
	);
}

render();
